# Texto Extraído

˅ **Webhook** 2/2 escopos

☑️ **Gerenciar dados de webhook** ⓘ
webhook_write

☑️ **Ler dados de webhook** ⓘ
webhook_read

---

# Descrição

Screenshot de uma seção de seleção de escopos (scopes) OAuth relacionados a Webhook.

A tela exibe:
- Categoria "Webhook" expandida (seta para baixo)
- Indicador "2/2 escopos" mostrando que ambos os escopos estão selecionados
- Dois escopos disponíveis, ambos com checkbox marcado (azul):
  1. **Gerenciar dados de webhook** (webhook_write)
     - Permite escrita/gerenciamento de webhooks
     - Ícone de informação (ⓘ) para mais detalhes
  2. **Ler dados de webhook** (webhook_read)
     - Permite leitura de dados de webhooks
     - Ícone de informação (ⓘ) para mais detalhes

Esta imagem documenta a seleção de permissões OAuth para webhooks, mostrando os dois escopos disponíveis: webhook_write e webhook_read.
